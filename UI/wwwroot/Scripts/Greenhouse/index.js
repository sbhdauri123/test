$(function () {

    //alert('Your screen width is ' + $(window).width());

    $("#slider1").responsiveSlides({
        auto: true,
        pager: false,
        nav: false,
        speed: 500,
        namespace: "callbacks",
    });


});

